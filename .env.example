# Public site URL used for sitemap, canonical URLs, and Open Graph tags.
# Local default. Production builds fall back to https://hard-water-fix.vercel.app
# (or VERCEL_PROJECT_PRODUCTION_URL) when this is unset, so sitemap.xml never
# ships localhost URLs.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
